$(document).ready(function(){ // cuando el documento este listo ".ready" osea cuando se termine de cargar todo
    $("p").click(function(){ // cuando se le da clic a p, se hace lo siguiente
        $(this).hide();         // cuando se le de clic a las p se oculten

    });
});